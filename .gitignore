# Generated content
audio/
temp_audio_segments/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
.env

# Personal Progress — checked in so state survives across machines/clones.
# The .example files are the empty templates; the live files are YOUR data.

# Live HA config mirror — has the real webhook_id; repo is public, never commit.
# Sanitized template lives in docs/home_assistant_knock_buttons.md.
docs/anna_knock_automation.yaml

# Studio watchdog — local runtime droppings, never repo state
.studio.lock
studio_watchdog.log
